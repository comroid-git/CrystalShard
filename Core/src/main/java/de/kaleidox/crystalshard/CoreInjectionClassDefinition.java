package de.kaleidox.crystalshard;

import java.util.Optional;

public class CoreInjectionClassDefinition extends Injector {
    @Override
    protected Optional<Class> findOverride(Class forClass) {
        Class ret = null;

        return Optional.ofNullable(ret);
    }
}
