import static java.lang.System.getenv

ext {
    artifactName = 'crystalshard'
    userbase = 'comroid-git'
    projectDescription = "Modular Java-Wrapper for the Discord Bot API"
    
    exportedProjects = [
            ':api',
            ':impl',
            ':core',
            ':util'
    ]

    snapshot = project.version.endsWith("-SNAPSHOT")

    sonatypeUsername = getenv("sonatypeUsername")
    sonatypePassword = getenv("sonatypePassword")
}

ext."signing.gnupg.passphrase" = getenv("gpgKeyPassword")
